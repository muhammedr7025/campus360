import 'package:flutter/material.dart';

// Color Palette
const Color primaryColor = Color(0xFF26A69A); // Teal (Primary color)
const Color secondaryColor = Color(0xFF80CBC4); // Light Teal (Secondary color)
const Color accentColor = Color(0xFF004D40); // Dark Teal (Contrast or accents)
const Color backgroundColor =
    Color(0xFFC8E6C9); // Light Green (Background or soft accents)
const Color textColor = Color(0xFF1F2937); // Dark Gray for text

// Text Styles
TextStyle titleTextStyle = TextStyle(
  fontSize: 28,
  fontWeight: FontWeight.bold,
  color: textColor,
);

TextStyle subtitleTextStyle = TextStyle(
  fontSize: 20,
  color: primaryColor,
  fontWeight: FontWeight.w600,
);

TextStyle buttonTextStyle = TextStyle(
  fontSize: 18,
  color: accentColor,
);

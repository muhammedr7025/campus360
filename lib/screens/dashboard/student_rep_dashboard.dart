import 'package:flutter/material.dart';

class StudentRepDashboard extends StatelessWidget {
  const StudentRepDashboard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
